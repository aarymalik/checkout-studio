# @checkout-studio/design-system

Studio design tokens, themes and the Tailwind layer. Produces values, not components.

**Layer 2.** May depend on: config, types, utils.

Does not contain: React components, and anything the published checkout renders.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
