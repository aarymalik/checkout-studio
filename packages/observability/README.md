# @checkout-studio/observability

Logging, metrics, tracing and RUM. The only package permitted to import a telemetry vendor SDK.

**Layer 2.** May depend on: config, types, utils.

Does not contain: business logic of any kind.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
