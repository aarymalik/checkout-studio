# @checkout-studio/plugin-sdk

The contracts every plugin implements and the registries the engines read.

**Layer 3.** May depend on: config, types, utils, schema, design-system, observability.

Does not contain: any concrete plugin, and any checkout concept.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
