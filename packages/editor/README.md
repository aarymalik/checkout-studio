# @checkout-studio/editor

The visual editing engine: state, history, canvas, drag and drop, inspector.

**Layer 5.** May depend on: config, types, utils, schema, design-system, observability, plugin-sdk, ui, hooks, renderer.

Does not contain: server code, and any knowledge of checkout — the editor edits documents.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
