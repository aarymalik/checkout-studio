# @checkout-studio/database

Prisma client and repositories. Every method is tenant-scoped.

**Layer 3.** May depend on: config, types, utils, schema, design-system, observability.

Does not contain: HTTP concerns, and any use from a client component.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
