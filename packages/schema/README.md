# @checkout-studio/schema

The checkout document: types, validation, migration and pure tree operations.

**Layer 2.** May depend on: config, types, utils.

Does not contain: React, DOM access, or any component-specific rule — those are injected by plugins.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
