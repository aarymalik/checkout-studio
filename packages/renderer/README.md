# @checkout-studio/renderer

The rendering engine: turns a checkout document into a rendered page.

**Layer 4.** May depend on: config, types, utils, schema, observability, plugin-sdk, hooks.

Does not contain: editor code, Studio UI, and any checkout logic — the checkout plugin contributes that.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
