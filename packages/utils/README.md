# @checkout-studio/utils

Pure utilities and shared Zod primitives. Runs in a browser, on a server, and in a test with equal indifference.

**Layer 1.** May depend on: config.

Does not contain: React, DOM access, Node built-ins, or anything with I/O.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
