# @checkout-studio/types

Shared TypeScript types with no runtime output: the API envelope and entity DTOs.

**Layer 1.** May depend on: config.

Does not contain: schema, theme, plugin or editor types — each is owned by the package that defines it.

See [docs/monorepo-structure.md](../../docs/monorepo-structure.md).
